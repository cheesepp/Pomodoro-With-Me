import 'package:flutter/material.dart';

// Default


  /// ----  Blue Theme  ----
  // final bluePrimary = Color(0xFF3F51B5);
  // final blueAccent = Color(0xFFFF9800);
  // final blueBackground = Color(0xFFFFFFFF);
  // final blueTheme = ThemeData(
  //     primaryColor: bluePrimary,
  //     accentColor: blueAccent,
  //     backgroundColor: blueBackground);

  /// ----  Beige Theme  ----
  const beigePrimary = Color(0xffc9ad7f);
  const beigeAccent = Color(0xfff6f7dd);
  final beigeTheme = ThemeData(
    backgroundColor: Colors.brown,
    primaryColor: beigePrimary,
    accentColor: beigeAccent,
    primarySwatch: Colors.brown,
    fontFamily:  'DancingScript',
  );

  final beigeLoraFont = ThemeData(
    backgroundColor: Colors.brown,
    primaryColor: beigePrimary,
    accentColor: beigeAccent,
    primarySwatch: Colors.brown,
    fontFamily:  'Lora',
  );

  /// ----  Green Theme  ----
  const greenPrimary = Color(0xffb6cec7);
  const greenAccent = Color(0xffd8e0bb);

  final greenTheme = ThemeData(
    backgroundColor: Colors.green,
    primaryColor: greenPrimary,
    accentColor: greenAccent,
    primarySwatch: Colors.green,
    fontFamily: 'DancingScript',
  );

  /// ----  Pink Theme  ----
  const pinkPrimary = Color(0xffffa4b6);
  const pinkAccent = Color(0xfff9d1d1);

  final pinkTheme = ThemeData(
    backgroundColor: Colors.pink,
    primaryColor: pinkPrimary,
    accentColor: pinkAccent,
    primarySwatch: Colors.pink,
    fontFamily: 'DancingScript',
  );

final pinkLoraFont = ThemeData(
  backgroundColor: Colors.pink,
  primaryColor: pinkPrimary,
  accentColor: pinkAccent,
  primarySwatch: Colors.pink,
  fontFamily: 'Lora',
);

